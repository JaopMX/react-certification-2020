import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  root: {
    width: 375,
    height: 400,
    marginTop: '1.5rem',
  },
  cardTitle: {
    color: 'black',
  },
});

export default useStyles;
